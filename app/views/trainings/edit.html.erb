<h1>編集画面</h1>

<%= form_with model:@training, local: true do |f| %>
  <h4>タイトル</h4>
  <%= f.text_field :title %>
  <h4>本文</h4>
  <%= f.text_area :body %>
  <%= f.submit '保存' %>
<% end %>

<div>
<%= link_to 'show', training_path(@training.id) %>|
<%= link_to 'back', trainings_path %>
</div>
